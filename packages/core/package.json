{
  "name": "@xixi/core",
  "version": "0.1.0",
  "description": "Core domain and storage logic for xixi",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "fs-extra": "^11.3.0",
    "js-yaml": "^4.1.0",
    "zod": "^3.24.2"
  }
}

